{
  "name": "halo-api-wrapper",
  "version": "1.0.1",
  "description": "Node wrapper for the Halo API. Returns a Promise that will return the JSON from the API. Source available at ",
  "main": "./src/halo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Googlrr",
  "license": "ISC",
  "dependencies": {
    "https": "^1.0.0"
  },
  "devDependencies": {
    "dotenv": "^4.0.0"
  }
}
